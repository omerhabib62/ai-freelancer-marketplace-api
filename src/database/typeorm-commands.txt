npm run typeorm migration:run -- -d ./src/database/data-source.ts

npm run typeorm migration:help

// Show all migrations and migration:show whether they have been run or not
npm run typeorm migration:show  -- -d ./src/database/data-source.ts 
// DAta source already passed in package.json cli script.

npm run typeorm migration:generate ./src/database/migrations/InitialMigration

//To clear everything stored in the cache.
npm run typeorm cache:clear

//For typeorm caching
https://typeorm.io/docs/query-builder/caching/ 

//For typeorm migrations
https://typeorm.io/docs/advanced-topics/migrations/

docker compose exec postgres psql -U postgres -c "CREATE DATABASE \"ai-freelancer-marketplace\";"

/// -f flag means "follow", so it will live-stream the logs like a console.
docker logs -f ai-freelancer-marketplace-api-1
